{
  // Faz a extensão Jest Orta usar o runner do Stencil (npm run test)
  "jest.jestCommandLine": "npm run test --",
  // Opcional: desabilite o coverage automático
  "jest.coverageEnabled": false,
  "conventionalCommits.scopes": [
    "icon",
    "brand",
    "field-number",
    "button",
    "tab",
    "steps",
    "icon-large",
    "switch",
    "date-picker",
    "field-date",
    "npm"
  ],
  // Opcional: desabilite o auto-run se preferir rodar só manualmente
  // "jest.autoRun": false
}
